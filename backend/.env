# Production Environment Variables

PORT=3000

# MongoDB connection string (double-check username, password, and cluster URL)
MONGODB_URI="mongodb+srv://college:akash123@cluster0.d7mvs.mongodb.net/hospitalDB?retryWrites=true&w=majority&appName=Cluster0"

# Client URL (your frontend URL — this looks correct if your frontend is hosted on Vercel)
CLIENT_URL=http://localhost:5173/

# Secret for JWT signing (replace with a long, randomly generated secure string)
JWT_SECRET=your_secure_secret_here
